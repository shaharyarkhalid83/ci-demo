# CI/CD Demo (Flask + Docker + ECS)

This is a minimal Flask application used to demonstrate a full AWS CI/CD pipeline:

- GitHub → CodePipeline (Source)
- CodeBuild (Tests + Docker build)
- ECR (Image storage)
- ECS (Deploy automatically)

## Local development

